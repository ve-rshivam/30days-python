# Write a script that prompts the user to enter hours and rate per hour. Calculate pay of the person?

hours = float(input("Enter the workhour: "))
rate = float(input("Enter rate per hour: "))

salary = hours * rate

print("Here is salary: ",salary)